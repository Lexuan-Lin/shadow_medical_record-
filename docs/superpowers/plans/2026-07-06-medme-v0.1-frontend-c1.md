# MedMe v0.1 Frontend C1 (Tauri shell + IPC) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一个能打开、并从真实 Vault 展示时间线的 Tauri v2 桌面应用:Rust `pipeline` 摄入 crate + core-model 只读方法 + Tauri 命令 + React 外壳(Sidebar + Timeline)。

**Architecture:** 新建 `packages/pipeline`(core-model + parser 之上的 `ingest`),CLI 与 Tauri 共用。`apps/desktop/src-tauri`(Rust)把 `Vault` 放进 `State<Mutex<Vault>>` 并暴露 `#[tauri::command]`;React(Vite+TS+Tailwind v4)只经 `src/api.ts` 调命令。本计划交付到"应用打开、侧栏、时间线显示真实文档";导入/搜索/查看器/设置留给 C2。

**Tech Stack:** Rust · Tauri v2 · React 19 · TypeScript · Vite · Tailwind v4(`@tailwindcss/vite`)· lucide-react · pnpm

## Global Constraints

- 代码/标识符/schema 英文;注释可中文。汇报讨论用中文。
- Rust:库无 `unwrap()`/`expect()` 生产路径(`pipeline` 用 `anyhow`;`core-model` 用 `MedmeError`);测试可 `unwrap`。
- Tauri 命令返回 `Result<T, String>`(错误转成人读字符串,`.map_err(|e| e.to_string())`);命令内不 `panic`/`unwrap`。
- UI 只经 `src/api.ts` 调 `invoke`,其余组件不直接 import `@tauri-apps/api`。
- 时间统一 RFC3339 UTC 字符串;DTO 是 UI↔Rust 契约,改 Rust DTO 必须同步 `src/types.ts`。
- 复用既有 `Vault` API(见下方 Interfaces),不改既有 schema。
- 每个 task 独立可测、独立提交;Rust 任务走 TDD。
- 设计语言参照:项目记忆 `medme-ui-design-language` + `assets/design-reference/medme-ui-prototype.zip`(slate 底 / blue-600 主 / emerald 信任色 / rounded-xl 卡片 / 中文主标+英文 mono 副标)。

### 既有 Vault API(Task 1/3/5 消费,来自已合入的 foundation 分支)
```rust
Vault::open(root: &Path) -> Result<Vault, MedmeError>
Vault::import(original_name: &str, mime: &str, bytes: &[u8]) -> Result<Import, MedmeError>   // Import{ source_file: SourceFile, deduped: bool }
Vault::has_document(source_file_id: i64) -> Result<bool, MedmeError>
Vault::add_document(NewDocument) -> Result<Document, MedmeError>
Vault::add_ocr(NewOcr) -> Result<i64, MedmeError>
Vault::search(query: &str, limit: usize) -> Result<Vec<SearchHit>, MedmeError>   // SearchHit{ document_id, title(分词后), snippet }
Vault::timeline() -> Result<Vec<TimelineEntry>, MedmeError>                       // TimelineEntry{ document_id, doc_date:Option<DateTime<Utc>>, doc_type:DocType, title:Option<String> }
// 类型:SourceFile{id,content_hash,original_name,mime_type,byte_size,storage_path,imported_at}
//      Document{id,source_file_id,doc_type:DocType,doc_date:Option<DateTime<Utc>>,title:Option<String>,language:Option<String>,page_count:i32,created_at}
//      DocType{LabReport,ImagingReport,DischargeSummary,Prescription,ClinicalNote,Pathology,Other,Unknown} 有 as_str()/from_str()
//      OcrBackendKind{Native,Onnx,Vlm}; NewDocument/NewOcr 见 foundation types.rs
// parser::extract(path:&Path) -> anyhow::Result<Extracted{text,page_count:i32,language:Option<String>,doc_date:Option<DateTime<Utc>>,doc_type:DocType}>
```

---

### Task 1: `packages/pipeline` crate — `ingest`

**Files:**
- Create: `packages/pipeline/Cargo.toml`
- Create: `packages/pipeline/src/lib.rs`
- Modify: `Cargo.toml`(workspace members 加 `packages/pipeline`)

**Interfaces:**
- Consumes: `Vault::{import,has_document,add_document,add_ocr}`, `parser::extract`, `core_model::{NewDocument,NewOcr,OcrBackendKind,DocType}`
- Produces:
  - `pipeline::mime_for(path: &Path) -> &'static str`
  - `pipeline::IngestStatus`(`New|Deduped|Backfilled|StoredNoText`,derive `Debug,Clone,PartialEq`)
  - `pipeline::IngestOutcome { source_file_id: i64, name: String, status: IngestStatus, doc_type: Option<DocType> }`(derive `Debug,Clone`)
  - `pipeline::ingest(vault: &Vault, path: &Path) -> anyhow::Result<IngestOutcome>`

- [ ] **Step 1: manifest + workspace 注册**

`packages/pipeline/Cargo.toml`:
```toml
[package]
name = "pipeline"
version = "0.1.0"
edition = "2021"

[dependencies]
core-model = { path = "../core-model" }
parser = { path = "../parser" }
anyhow.workspace = true

[dev-dependencies]
tempfile.workspace = true
```
根 `Cargo.toml` 的 `members` 追加 `"packages/pipeline"`(保持其余不变)。

- [ ] **Step 2: 写失败测试**

`packages/pipeline/src/lib.rs` 末尾:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use core_model::Vault;
    use std::io::Write;

    fn tmp_txt(dir: &std::path::Path, name: &str, body: &str) -> std::path::PathBuf {
        let p = dir.join(name);
        let mut f = std::fs::File::create(&p).unwrap();
        f.write_all(body.as_bytes()).unwrap();
        p
    }

    #[test]
    fn ingest_new_then_dedup() {
        let vdir = tempfile::tempdir().unwrap();
        let fdir = tempfile::tempdir().unwrap();
        let v = Vault::open(vdir.path()).unwrap();
        let f = tmp_txt(fdir.path(), "report.txt", "出院记录 2023-05-01 肌酐 Creatinine 120");

        let o1 = ingest(&v, &f).unwrap();
        assert_eq!(o1.status, IngestStatus::New);
        assert!(o1.doc_type.is_some());

        let o2 = ingest(&v, &f).unwrap();
        assert_eq!(o2.status, IngestStatus::Deduped);       // 已存在且已索引
        assert_eq!(o1.source_file_id, o2.source_file_id);

        // 时间线只有一条
        assert_eq!(v.timeline().unwrap().len(), 1);
    }

    #[test]
    fn ingest_stored_no_text_for_unsupported() {
        let vdir = tempfile::tempdir().unwrap();
        let fdir = tempfile::tempdir().unwrap();
        let v = Vault::open(vdir.path()).unwrap();
        // .bin 扩展名 → parser::extract 报错 → StoredNoText,但文件已入 CAS
        let p = fdir.path().join("scan.bin");
        std::fs::write(&p, b"\x00\x01\x02rawbytes").unwrap();

        let o = ingest(&v, &p).unwrap();
        assert_eq!(o.status, IngestStatus::StoredNoText);
        assert!(!v.has_document(o.source_file_id).unwrap());  // 没建 document
        assert_eq!(v.timeline().unwrap().len(), 0);
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test -p pipeline`
Expected: 编译失败(`ingest`/`IngestStatus`/`IngestOutcome` 未定义)。

- [ ] **Step 4: 实现**

`packages/pipeline/src/lib.rs`(测试上方):
```rust
use std::path::Path;
use core_model::{Vault, NewDocument, NewOcr, OcrBackendKind, DocType};

pub fn mime_for(path: &Path) -> &'static str {
    match path.extension().and_then(|e| e.to_str()).unwrap_or("").to_lowercase().as_str() {
        "pdf" => "application/pdf",
        "txt" => "text/plain",
        "png" => "image/png",
        "jpg" | "jpeg" => "image/jpeg",
        "tif" | "tiff" => "image/tiff",
        _ => "application/octet-stream",
    }
}

#[derive(Debug, Clone, PartialEq)]
pub enum IngestStatus { New, Deduped, Backfilled, StoredNoText }

#[derive(Debug, Clone)]
pub struct IngestOutcome {
    pub source_file_id: i64,
    pub name: String,
    pub status: IngestStatus,
    pub doc_type: Option<DocType>,
}

/// 导入一个文件:存 CAS(去重)→ 若尚无 document 则抽文本层并建 document/ocr。
/// 抽取失败(如扫描图片)不致命 → StoredNoText(原文件已永存,留待后续 OCR 补索引)。
pub fn ingest(vault: &Vault, path: &Path) -> anyhow::Result<IngestOutcome> {
    let bytes = std::fs::read(path)?;
    let name = path.file_name().and_then(|n| n.to_str()).unwrap_or("unknown").to_string();
    let imp = vault.import(&name, mime_for(path), &bytes)?;
    let sid = imp.source_file.id;

    if imp.deduped && vault.has_document(sid)? {
        return Ok(IngestOutcome { source_file_id: sid, name, status: IngestStatus::Deduped, doc_type: None });
    }

    match parser::extract(path) {
        Ok(e) => {
            let doc = vault.add_document(NewDocument {
                source_file_id: sid,
                doc_type: e.doc_type.clone(),
                doc_date: e.doc_date,
                title: Some(name.clone()),
                language: e.language,
                page_count: e.page_count,
            })?;
            vault.add_ocr(NewOcr {
                document_id: doc.id,
                page_no: 1,
                backend: OcrBackendKind::Native,
                model_version: "text-layer".into(),
                text: e.text,
                confidence: None,
            })?;
            let status = if imp.deduped { IngestStatus::Backfilled } else { IngestStatus::New };
            Ok(IngestOutcome { source_file_id: sid, name, status, doc_type: Some(doc.doc_type) })
        }
        Err(_) => Ok(IngestOutcome { source_file_id: sid, name, status: IngestStatus::StoredNoText, doc_type: None }),
    }
}
```
注:`_ = DocType;` 若 `DocType` 未直接使用会有 unused import——它用于 `IngestOutcome.doc_type: Option<DocType>`,已使用。

- [ ] **Step 5: 运行确认通过**

Run: `cargo test -p pipeline`
Expected: 2 测试 PASS。

- [ ] **Step 6: clippy + 提交**

Run: `cargo clippy -p pipeline --all-targets -- -D warnings`(clean)
```bash
git add Cargo.toml packages/pipeline
git commit -m "feat(pipeline): shared ingest crate (import + text-layer index)"
```

---

### Task 2: CLI 改用 `pipeline::ingest`(消重复)

**Files:**
- Modify: `apps/cli/Cargo.toml`(加 `pipeline` 依赖)
- Modify: `apps/cli/src/main.rs`(import 分支改调 `pipeline::ingest`,删本地 `mime_for` 与内联编排)

**Interfaces:**
- Consumes: `pipeline::{ingest, IngestStatus}`

- [ ] **Step 1: 加依赖**

`apps/cli/Cargo.toml` 的 `[dependencies]` 加:
```toml
pipeline = { path = "../../packages/pipeline" }
```

- [ ] **Step 2: 改 import 分支**

`apps/cli/src/main.rs`:删除本地 `fn mime_for(...)`;把 `Cmd::Import { files }` 分支的循环体整体替换为:
```rust
        Cmd::Import { files } => {
            for f in files {
                let o = pipeline::ingest(&vault, &f)?;
                let line = match o.status {
                    pipeline::IngestStatus::New =>
                        format!("import {} (id={}, type={})", o.name, o.source_file_id,
                                o.doc_type.as_ref().map(|d| d.as_str()).unwrap_or("unknown")),
                    pipeline::IngestStatus::Backfilled =>
                        format!("index  {} (backfilled, id={})", o.name, o.source_file_id),
                    pipeline::IngestStatus::Deduped =>
                        format!("dedup  {} (already stored & indexed, id={})", o.name, o.source_file_id),
                    pipeline::IngestStatus::StoredNoText =>
                        format!("import {} (stored, no text layer, id={})", o.name, o.source_file_id),
                };
                println!("{line}");
            }
        }
```
其余子命令(`Search`/`Timeline`)与 `use` 保持不变(若 `NewDocument/NewOcr/OcrBackendKind` 的 import 变为未使用,一并删除以保持 clippy clean)。

- [ ] **Step 3: 手动回归**

Run:
```bash
cargo build -p medme-cli
V=$(mktemp -d)
./target/debug/medme --vault "$V" import packages/parser/tests/fixtures/sample.txt
./target/debug/medme --vault "$V" import packages/parser/tests/fixtures/sample.txt
./target/debug/medme --vault "$V" search 肌酐
./target/debug/medme --vault "$V" timeline
```
Expected:首次 `import ... type=discharge_summary`;二次 `dedup ... already stored & indexed`;`search 肌酐` 命中 1 条;`timeline` 显示 `2023-05-01 [discharge_summary] sample.txt`。行为与重构前一致。

- [ ] **Step 4: clippy + 提交**

Run: `cargo clippy --workspace --all-targets -- -D warnings`(clean)
```bash
git add apps/cli
git commit -m "refactor(cli): use pipeline::ingest (single ingest path)"
```

---

### Task 3: core-model 只读方法(供命令层)

**Files:**
- Modify: `packages/core-model/src/query.rs`(加 3 个只读方法 + 测试)

**Interfaces:**
- Produces(均在 `impl Vault`):
  - `Vault::document_by_id(id: i64) -> Result<Option<Document>, MedmeError>`
  - `Vault::source_file_by_id(id: i64) -> Result<Option<SourceFile>, MedmeError>`
  - `Vault::ocr_text(document_id: i64) -> Result<String, MedmeError>`(按 `page_no` 升序拼接各页 `text`,以 `\n` 连接)

- [ ] **Step 1: 写失败测试**

在 `packages/core-model/src/query.rs` 的 `#[cfg(test)] mod tests` 内追加(复用文件里已有的 `seed` 辅助——它 import 文件、建 document+ocr):
```rust
    #[test]
    fn reads_document_source_and_ocr_text() {
        let dir = tempfile::tempdir().unwrap();
        let v = Vault::open(dir.path()).unwrap();
        seed(&v, "血常规", "肌酐 Creatinine 120", Some("2023-05-01T00:00:00Z"));

        let doc = v.timeline().unwrap()[0].clone();
        let full = v.document_by_id(doc.document_id).unwrap().unwrap();
        assert_eq!(full.title.as_deref(), Some("血常规"));

        let sf = v.source_file_by_id(full.source_file_id).unwrap().unwrap();
        assert_eq!(sf.original_name, "血常规");

        let text = v.ocr_text(doc.document_id).unwrap();
        assert!(text.contains("Creatinine"));

        // 不存在的 id → None / 空
        assert!(v.document_by_id(99999).unwrap().is_none());
        assert!(v.source_file_by_id(99999).unwrap().is_none());
        assert_eq!(v.ocr_text(99999).unwrap(), "");
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test -p core-model reads_document_source_and_ocr_text`
Expected: 编译失败(方法未定义)。

- [ ] **Step 3: 实现**

在 `query.rs` 的 `impl Vault` 块内加入(`use` 顶部确保有 `use crate::types::{SourceFile, Document, parse_dt}; use crate::DocType; use rusqlite::OptionalExtension;`——若已存在勿重复):
```rust
    pub fn document_by_id(&self, id: i64) -> Result<Option<Document>, MedmeError> {
        let row = self.conn().query_row(
            "SELECT id, source_file_id, doc_type, doc_date, title, language, page_count, created_at
             FROM document WHERE id = ?1",
            [id],
            |r| Ok(Document {
                id: r.get(0)?,
                source_file_id: r.get(1)?,
                doc_type: DocType::from_str(&r.get::<_, String>(2)?),
                doc_date: r.get::<_, Option<String>>(3)?.map(parse_dt),
                title: r.get(4)?,
                language: r.get(5)?,
                page_count: r.get(6)?,
                created_at: parse_dt(r.get::<_, String>(7)?),
            }),
        ).optional()?;
        Ok(row)
    }

    pub fn source_file_by_id(&self, id: i64) -> Result<Option<SourceFile>, MedmeError> {
        let row = self.conn().query_row(
            "SELECT id, content_hash, original_name, mime_type, byte_size, storage_path, imported_at
             FROM source_file WHERE id = ?1",
            [id],
            |r| Ok(SourceFile {
                id: r.get(0)?,
                content_hash: r.get(1)?,
                original_name: r.get(2)?,
                mime_type: r.get(3)?,
                byte_size: r.get(4)?,
                storage_path: r.get(5)?,
                imported_at: parse_dt(r.get::<_, String>(6)?),
            }),
        ).optional()?;
        Ok(row)
    }

    pub fn ocr_text(&self, document_id: i64) -> Result<String, MedmeError> {
        let mut stmt = self.conn().prepare(
            "SELECT text FROM ocr_result WHERE document_id = ?1 ORDER BY page_no ASC",
        )?;
        let rows = stmt.query_map([document_id], |r| r.get::<_, String>(0))?;
        let mut parts = Vec::new();
        for r in rows { parts.push(r?); }
        Ok(parts.join("\n"))
    }
```
若 `SourceFile`/`Document` 未在 `query.rs` 引入,加 `use crate::types::{SourceFile, Document};`(`parse_dt` 已在 Task 5(foundation)引入)。

- [ ] **Step 4: 运行确认通过**

Run: `cargo test -p core-model`
Expected: 全部 PASS(含新测试)。

- [ ] **Step 5: clippy + 提交**

Run: `cargo clippy -p core-model --all-targets -- -D warnings`(clean)
```bash
git add packages/core-model/src/query.rs
git commit -m "feat(core-model): document_by_id / source_file_by_id / ocr_text reads"
```

---

### Task 4: Tauri v2 应用脚手架(apps/desktop)

> 本 task 用官方脚手架生成当前版本配置,再做少量集成改动——**不手写全套 Tauri 配置**(版本敏感,易错)。交付:`pnpm tauri dev` 能打开一个空窗口。

**Files:**
- Create: `apps/desktop/**`(脚手架生成:`package.json`、`vite.config.ts`、`index.html`、`src/`、`src-tauri/`)
- Modify: `Cargo.toml`(workspace members 加 `apps/desktop/src-tauri`;若冲突见下)
- Create: `apps/desktop/.gitignore`(node_modules、dist、target)

**Interfaces:**
- Produces: 可运行的 Tauri v2 外壳;`src-tauri` crate 名记为 `medme-desktop`(供 Task 5 加命令)。

- [ ] **Step 1: 生成脚手架**

在 `apps/` 下用官方工具生成(React + TS + Vite 模板,pnpm):
```bash
cd apps
pnpm create tauri-app@latest desktop --template react-ts --manager pnpm --yes
cd desktop
pnpm install
```
若交互式无法非交互:手动选择 `React` / `TypeScript` / `pnpm` / Vite。生成后 `apps/desktop/src-tauri/` 为 Rust crate,`apps/desktop/src/` 为 React。

- [ ] **Step 2: 集成到工作区(处理 profile 冲突)**

Tauri 生成的 `apps/desktop/src-tauri/Cargo.toml` 常含 `[profile.*]`,而 workspace 只允许根 `Cargo.toml` 定义 profile。二选一:
- **推荐**:把 `apps/desktop/src-tauri` 加入根 `Cargo.toml` 的 `members`,并把 src-tauri 里的任何 `[profile.*]` 段**移到根** `Cargo.toml`(若根没有则整段搬过去);src-tauri 的 `Cargo.toml` 删除 `[profile.*]`。
- 确认 src-tauri crate 名:把 `apps/desktop/src-tauri/Cargo.toml` 的 `[package] name` 设为 `medme-desktop`(若脚手架用了别名,统一改;`tauri.conf.json` 的 `identifier` 保持脚手架值,可改成 `com.medme.app`)。

- [ ] **Step 3: .gitignore**

`apps/desktop/.gitignore`:
```
node_modules
dist
/src-tauri/target
```
(根 `.gitignore` 已忽略 `/target`;这里补 src-tauri 的 target 与前端产物。)

- [ ] **Step 4: 冒烟运行**

Run:
```bash
cd apps/desktop && pnpm tauri dev
```
Expected:编译 Rust + 启动 Vite,弹出一个桌面窗口显示脚手架默认页。确认无编译错误后关闭窗口(Ctrl-C)。
> 首次编译 Tauri 依赖较慢,属正常;若报缺少系统依赖,macOS 通常只需已装的 Xcode CLT(本机已具备)。

- [ ] **Step 5: 工作区构建校验 + 提交**

Run: `cargo build --workspace`(确认 src-tauri 纳入后整个工作区仍可构建)
```bash
git add Cargo.toml apps/desktop
git commit -m "chore(desktop): scaffold Tauri v2 app (react-ts) integrated into workspace"
```
> 注:若 `git add apps/desktop` 会带上 `node_modules`,确认上面的 `.gitignore` 已生效(`git status` 不应出现 node_modules)。

---

### Task 5: Tauri 命令 + DTO(包住 Vault)

**Files:**
- Modify: `apps/desktop/src-tauri/Cargo.toml`(加 `core-model`、`pipeline`、`serde` 依赖)
- Create: `apps/desktop/src-tauri/src/dto.rs`(Serialize DTO + 从 core 类型的转换)
- Create: `apps/desktop/src-tauri/src/commands.rs`(命令 + Vault 状态)
- Modify: `apps/desktop/src-tauri/src/lib.rs`(注册状态与命令)

**Interfaces:**
- Consumes: `core_model::Vault` 全部只读/写方法(见 Global Constraints)、`pipeline::ingest`
- Produces(命令,`invoke` 名):`list_timeline`、`search`、`get_document`、`import_paths`、`read_source_bytes`、`export_vault`(export 本 task 先返回计数占位,真正打包在 C2/后续);DTO 结构见下。

- [ ] **Step 1: 加依赖**

`apps/desktop/src-tauri/Cargo.toml` 的 `[dependencies]` 加:
```toml
core-model = { path = "../../../packages/core-model" }
pipeline = { path = "../../../packages/pipeline" }
serde = { version = "1", features = ["derive"] }
```
(`serde_json`、`tauri` 脚手架已带。)

- [ ] **Step 2: DTO**

`apps/desktop/src-tauri/src/dto.rs`:
```rust
use serde::Serialize;
use core_model::{Document, SourceFile};

#[derive(Serialize)]
pub struct DocumentSummary {
    pub id: i64,
    pub doc_type: String,
    pub doc_date: Option<String>,   // RFC3339
    pub title: Option<String>,
    pub page_count: i32,
}
impl From<&Document> for DocumentSummary {
    fn from(d: &Document) -> Self {
        DocumentSummary {
            id: d.id,
            doc_type: d.doc_type.as_str().to_string(),
            doc_date: d.doc_date.map(|x| x.to_rfc3339()),
            title: d.title.clone(),
            page_count: d.page_count,
        }
    }
}

#[derive(Serialize)]
pub struct SourceFileMeta {
    pub id: i64,
    pub original_name: String,
    pub mime_type: String,
    pub byte_size: i64,
    pub imported_at: String,
}
impl From<&SourceFile> for SourceFileMeta {
    fn from(s: &SourceFile) -> Self {
        SourceFileMeta {
            id: s.id,
            original_name: s.original_name.clone(),
            mime_type: s.mime_type.clone(),
            byte_size: s.byte_size,
            imported_at: s.imported_at.to_rfc3339(),
        }
    }
}

#[derive(Serialize)]
pub struct SearchResult { pub document: DocumentSummary, pub snippet: String }

#[derive(Serialize)]
pub struct DocumentDetail {
    pub document: DocumentSummary,
    pub source_file: SourceFileMeta,
    pub ocr_text: String,
}

#[derive(Serialize)]
pub struct ImportOutcome {
    pub name: String,
    pub source_file_id: i64,
    pub status: String,          // new|backfilled|deduped|stored_no_text
    pub doc_type: Option<String>,
}

#[derive(Serialize)]
pub struct ExportSummary { pub file_count: i64, pub byte_size: i64 }
```

- [ ] **Step 3: 命令 + 状态**

`apps/desktop/src-tauri/src/commands.rs`:
```rust
use std::path::PathBuf;
use std::sync::Mutex;
use tauri::State;
use core_model::Vault;
use crate::dto::*;

pub struct AppState { pub vault: Mutex<Vault> }

fn lock<'a>(s: &'a State<AppState>) -> Result<std::sync::MutexGuard<'a, Vault>, String> {
    s.vault.lock().map_err(|_| "vault lock poisoned".to_string())
}

#[tauri::command]
pub fn list_timeline(state: State<AppState>) -> Result<Vec<DocumentSummary>, String> {
    let v = lock(&state)?;
    let entries = v.timeline().map_err(|e| e.to_string())?;
    let mut out = Vec::new();
    for e in entries {
        // 用 document_by_id 拿完整字段(timeline 的 title 已是真实值,但这里统一走 summary)
        if let Some(doc) = v.document_by_id(e.document_id).map_err(|e| e.to_string())? {
            out.push(DocumentSummary::from(&doc));
        }
    }
    Ok(out)
}

#[tauri::command]
pub fn search(state: State<AppState>, query: String, limit: usize) -> Result<Vec<SearchResult>, String> {
    let v = lock(&state)?;
    let hits = v.search(&query, limit).map_err(|e| e.to_string())?;
    let mut out = Vec::new();
    for h in hits {
        // 取真实 document.title(而非 SearchHit 里的分词 title)
        if let Some(doc) = v.document_by_id(h.document_id).map_err(|e| e.to_string())? {
            out.push(SearchResult { document: DocumentSummary::from(&doc), snippet: h.snippet });
        }
    }
    Ok(out)
}

#[tauri::command]
pub fn get_document(state: State<AppState>, id: i64) -> Result<DocumentDetail, String> {
    let v = lock(&state)?;
    let doc = v.document_by_id(id).map_err(|e| e.to_string())?
        .ok_or_else(|| format!("document {id} not found"))?;
    let sf = v.source_file_by_id(doc.source_file_id).map_err(|e| e.to_string())?
        .ok_or_else(|| "source_file missing".to_string())?;
    let text = v.ocr_text(id).map_err(|e| e.to_string())?;
    Ok(DocumentDetail {
        document: DocumentSummary::from(&doc),
        source_file: SourceFileMeta::from(&sf),
        ocr_text: text,
    })
}

#[tauri::command]
pub fn import_paths(state: State<AppState>, paths: Vec<String>) -> Result<Vec<ImportOutcome>, String> {
    let v = lock(&state)?;
    let mut out = Vec::new();
    for p in paths {
        let o = pipeline::ingest(&v, std::path::Path::new(&p)).map_err(|e| e.to_string())?;
        let status = match o.status {
            pipeline::IngestStatus::New => "new",
            pipeline::IngestStatus::Backfilled => "backfilled",
            pipeline::IngestStatus::Deduped => "deduped",
            pipeline::IngestStatus::StoredNoText => "stored_no_text",
        }.to_string();
        out.push(ImportOutcome {
            name: o.name, source_file_id: o.source_file_id, status,
            doc_type: o.doc_type.map(|d| d.as_str().to_string()),
        });
    }
    Ok(out)
}

#[tauri::command]
pub fn read_source_bytes(state: State<AppState>, id: i64) -> Result<Vec<u8>, String> {
    let v = lock(&state)?;
    let sf = v.source_file_by_id(id).map_err(|e| e.to_string())?
        .ok_or_else(|| format!("source_file {id} not found"))?;
    let path = v.root_join(&sf.storage_path);   // 见下方 note:需 core-model 暴露 root 拼接
    std::fs::read(&path).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn export_vault(_state: State<AppState>, _dest_path: String) -> Result<ExportSummary, String> {
    // C2/后续:真正打包 objects/ + JSON 清单。此处占位返回 0,避免未实现命令。
    Ok(ExportSummary { file_count: 0, byte_size: 0 })
}
```
> **note(core-model 依赖)**:`read_source_bytes` 需要把 `storage_path`(相对)拼到 vault root。为此在 `packages/core-model/src/cas.rs` 的 `impl Vault` 增加一个只读方法并提交(小改,可并入本 task 的 core-model 依赖):
> ```rust
> pub fn root_join(&self, rel: &str) -> std::path::PathBuf { self.root().join(rel) }
> ```
> `root()` 已是 `pub(crate)`;`root_join` 设为 `pub`。

- [ ] **Step 4: 注册状态与命令**

改 `apps/desktop/src-tauri/src/lib.rs`(脚手架的 `run()` 函数),打开 vault 于 app-data 目录并注册:
```rust
mod dto;
mod commands;
use commands::AppState;
use std::sync::Mutex;
use core_model::Vault;
use tauri::Manager;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())   // 脚手架默认插件,保留原样;若脚手架未加则删此行
        .setup(|app| {
            let dir = app.path().app_data_dir().expect("app data dir");
            std::fs::create_dir_all(&dir).ok();
            let vault = Vault::open(&dir.join("vault")).expect("open vault");
            app.manage(AppState { vault: Mutex::new(vault) });
            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            commands::list_timeline,
            commands::search,
            commands::get_document,
            commands::import_paths,
            commands::read_source_bytes,
            commands::export_vault,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```
> `setup` 里的 `expect` 属启动不变量(拿不到 app-data / 打不开 vault 则无法运行),可接受并有文档;命令内部一律 `Result<_, String>` 不 panic。若脚手架的插件名不同(如无 `tauri_plugin_opener`),按实际保留其默认 `.plugin(...)` 行。

- [ ] **Step 5: 构建校验**

Run:
```bash
cargo build -p medme-desktop
cargo clippy -p medme-desktop --all-targets -- -D warnings
```
Expected:编译通过、clippy clean。(命令层无单测——逻辑薄,靠 `pipeline`/`core-model` 既有测试;此处只保证类型正确、能编译。)

- [ ] **Step 6: 提交**

```bash
git add apps/desktop/src-tauri packages/core-model/src/cas.rs
git commit -m "feat(desktop): Vault-backed Tauri commands + DTOs"
```

---

### Task 6: React api 层 + 外壳 + 时间线(端到端可见)

**Files:**
- Create: `apps/desktop/src/types.ts`(镜像 DTO)
- Create: `apps/desktop/src/api.ts`(唯一 invoke 封装)
- Replace: `apps/desktop/src/App.tsx`(外壳:Sidebar + Timeline)
- Create: `apps/desktop/src/components/Sidebar.tsx`
- Create: `apps/desktop/src/components/Timeline.tsx`
- Modify: `apps/desktop/src/main.tsx` / CSS(接入 Tailwind v4)

**Interfaces:**
- Consumes: 命令 `list_timeline`(Task 5)
- Produces: 可运行 UI;`api.listTimeline()` 等函数(供 C2 复用)

- [ ] **Step 1: 接入 Tailwind v4**

装依赖并配置:
```bash
cd apps/desktop
pnpm add lucide-react
pnpm add -D tailwindcss @tailwindcss/vite
```
`apps/desktop/vite.config.ts`:在 plugins 里加入 `@tailwindcss/vite`:
```ts
import tailwindcss from "@tailwindcss/vite";
// plugins: [react(), tailwindcss()]  ← 把 tailwindcss() 加进现有 plugins 数组
```
`apps/desktop/src/`(主 CSS,脚手架通常是 `App.css` 或 `index.css`——用被 `main.tsx` import 的那个)首行改为:
```css
@import "tailwindcss";
```
删除脚手架默认的示例样式(保留字体基线即可)。

- [ ] **Step 2: 类型镜像**

`apps/desktop/src/types.ts`:
```ts
export interface DocumentSummary {
  id: number;
  doc_type: string;
  doc_date: string | null;   // RFC3339
  title: string | null;
  page_count: number;
}
export interface SourceFileMeta {
  id: number; original_name: string; mime_type: string; byte_size: number; imported_at: string;
}
export interface SearchResult { document: DocumentSummary; snippet: string; }
export interface DocumentDetail { document: DocumentSummary; source_file: SourceFileMeta; ocr_text: string; }
export interface ImportOutcome { name: string; source_file_id: number; status: string; doc_type: string | null; }
export interface ExportSummary { file_count: number; byte_size: number; }
```

- [ ] **Step 3: api 封装**

`apps/desktop/src/api.ts`:
```ts
import { invoke } from "@tauri-apps/api/core";
import type { DocumentSummary, SearchResult, DocumentDetail, ImportOutcome, ExportSummary } from "./types";

export const api = {
  listTimeline: () => invoke<DocumentSummary[]>("list_timeline"),
  search: (query: string, limit = 30) => invoke<SearchResult[]>("search", { query, limit }),
  getDocument: (id: number) => invoke<DocumentDetail>("get_document", { id }),
  importPaths: (paths: string[]) => invoke<ImportOutcome[]>("import_paths", { paths }),
  readSourceBytes: (id: number) => invoke<number[]>("read_source_bytes", { id }),
  exportVault: (destPath: string) => invoke<ExportSummary>("export_vault", { destPath }),
};
```

- [ ] **Step 4: Sidebar(设计语言,真 logo 待 C2 切图,先用 lucide + 字标)**

`apps/desktop/src/components/Sidebar.tsx`:
```tsx
import { Activity, ShieldCheck } from "lucide-react";

export default function Sidebar({ count }: { count: number }) {
  return (
    <div className="w-72 bg-white border-r border-slate-200 flex flex-col h-screen text-slate-600 select-none shrink-0">
      <div className="p-6 border-b border-slate-200">
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded-xl bg-blue-50 flex items-center justify-center text-blue-600 border border-blue-100">
            <ShieldCheck className="w-6 h-6" />
          </div>
          <div>
            <div className="flex items-center gap-1.5">
              <span className="font-bold text-xl text-blue-600 tracking-tight">MedMe</span>
              <span className="font-bold text-xl text-slate-950">医我</span>
            </div>
            <span className="text-[10px] font-mono text-slate-400 tracking-widest uppercase block mt-0.5">
              Personal Health Vault
            </span>
          </div>
        </div>
      </div>
      <nav className="flex-1 p-4">
        <div className="w-full flex items-center justify-between p-3.5 rounded-xl bg-blue-50 text-blue-700 border border-blue-100/40">
          <div className="flex items-center gap-3">
            <Activity className="w-5 h-5 text-blue-600" />
            <div>
              <span className="text-sm font-medium block text-blue-900">生命时间线</span>
              <span className="text-[10px] font-mono text-slate-400 block">Medical Lifeline</span>
            </div>
          </div>
          <span className="px-2 py-0.5 rounded-full text-[10px] font-bold font-mono bg-blue-600 text-white">{count}</span>
        </div>
      </nav>
      <div className="p-4 border-t border-slate-200 text-[10px] font-mono text-slate-400 flex justify-between">
        <span>© MedMe Team 2026</span><span>v0.1</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Timeline 视图**

`apps/desktop/src/components/Timeline.tsx`:
```tsx
import type { DocumentSummary } from "../types";

const TYPE_LABEL: Record<string, string> = {
  lab_report: "化验", imaging_report: "检查", discharge_summary: "出院",
  prescription: "处方", clinical_note: "病历", pathology: "病理",
  other: "其他", unknown: "未分类",
};
const TYPE_ACCENT: Record<string, string> = {
  lab_report: "border-blue-400", imaging_report: "border-amber-400",
  prescription: "border-emerald-400", discharge_summary: "border-indigo-400",
};

function fmtDate(d: string | null): string {
  if (!d) return "无日期";
  return d.slice(0, 10);
}

export default function Timeline({ docs }: { docs: DocumentSummary[] }) {
  if (docs.length === 0) {
    return (
      <div className="flex-1 flex items-center justify-center text-slate-400 text-sm">
        还没有记录。导入病历后,这里会按时间显示你的生命时间线。
      </div>
    );
  }
  return (
    <div className="flex-1 overflow-y-auto bg-slate-50 p-8">
      <div className="max-w-3xl mx-auto space-y-3">
        <h1 className="text-lg font-bold text-slate-900 mb-4">生命时间线
          <span className="ml-2 text-xs font-mono text-slate-500">{docs.length} 份</span>
        </h1>
        {docs.map((d) => (
          <div key={d.id}
               className={`bg-white border border-slate-200 border-l-4 ${TYPE_ACCENT[d.doc_type] ?? "border-slate-300"} rounded-2xl p-4 shadow-sm hover:shadow-md transition-all`}>
            <div className="flex items-center justify-between">
              <span className="font-medium text-slate-800">{d.title ?? "(无标题)"}</span>
              <span className="text-xs font-mono text-slate-500">{fmtDate(d.doc_date)}</span>
            </div>
            <span className="text-[11px] font-mono px-2 py-0.5 rounded bg-slate-100 text-slate-600 mt-1 inline-block">
              {TYPE_LABEL[d.doc_type] ?? d.doc_type}
            </span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 6: App 外壳**

`apps/desktop/src/App.tsx`(整体替换):
```tsx
import { useEffect, useState } from "react";
import Sidebar from "./components/Sidebar";
import Timeline from "./components/Timeline";
import { api } from "./api";
import type { DocumentSummary } from "./types";

export default function App() {
  const [docs, setDocs] = useState<DocumentSummary[]>([]);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    api.listTimeline().then(setDocs).catch((e) => setError(String(e)));
  }, []);

  return (
    <div className="w-screen h-screen flex bg-slate-50 overflow-hidden text-slate-800">
      <Sidebar count={docs.length} />
      <div className="flex-1 flex flex-col h-full overflow-hidden">
        {error
          ? <div className="flex-1 flex items-center justify-center text-rose-600 text-sm">加载失败:{error}</div>
          : <Timeline docs={docs} />}
      </div>
    </div>
  );
}
```

- [ ] **Step 7: 构建冒烟 + 手动 E2E**

Run:
```bash
cd apps/desktop
pnpm tsc --noEmit          # 类型检查通过
# 先用 CLI 往桌面 app 的同一 vault 塞一条数据,便于验证时间线渲染:
#   桌面 app 的 vault 在 app-data 目录;手动 E2E 直接用 app 内导入更简单——本 task 暂无导入 UI,
#   故改为:确认空态渲染 + 无报错;导入 UI 在 C2,届时端到端验证时间线出现。
pnpm tauri dev
```
Expected:
- `tsc --noEmit` 无错误。
- 应用打开:左侧 Sidebar 显示 "MedMe 医我 / Personal Health Vault" 与计数徽标;主区显示空态文案(vault 初次为空)。
- 控制台无红色错误(`list_timeline` 命令成功返回 `[]`)。
> 端到端"时间线显示真实文档"需要导入 UI(C2)或手动往该 vault 注入数据;本 task 的验收是**外壳 + 命令链路打通 + 空态正确**。若想立即看到卡片:可临时用 `medme` CLI 指向同一 app-data vault 路径导入一份 fixture 再重开应用(可选)。

- [ ] **Step 8: 提交**

```bash
git add apps/desktop/src apps/desktop/vite.config.ts apps/desktop/package.json apps/desktop/pnpm-lock.yaml
git commit -m "feat(desktop): React shell (Sidebar + Timeline) over Vault via api.ts"
```

---

## 后续(不在本计划内)

- **Plan C2 — UI 视图完善**:Import(拖拽 + `tauri-plugin-dialog` 文件选择)、Search、Document Viewer(原件 ↔ OCR 对照,渲染 `read_source_bytes`)、Settings + 真 `export_vault`(打包 objects/ + JSON 清单);从 logo 图纸切出独立 mark(SVG/PNG)替换 Sidebar 占位、生成 Tauri 应用图标;设计语言细化(motion 过渡、DocType 分组头)。
- **Plan B — OCR onnx**:让 `StoredNoText` 的图片/扫描件经 onnx 后端补索引 + `reindex` 命令(见记忆 `medme-planb-backlog`)。

## Self-Review

- **Spec 覆盖([008](../../008_Frontend_Tauri.md))**:pipeline 重构(T1)+ CLI 共用(T2)· core-model 只读方法(T3)· Tauri 脚手架(T4)· 命令+DTO+State(T5)· api.ts 单点+手写 types+外壳+时间线(T6)。Import/Search/Viewer/Settings/真export/logo 切图 = 明确移入 C2(见"后续"),非遗漏。
- **占位符**:`export_vault` 返回 0 是**有意的显式占位**(避免注册未实现命令导致 invoke 报错),已注明 C2 补;非计划占位符缺陷。T4 因 Tauri 版本敏感采用"脚手架+适配"而非逐行代码,已说明理由并给出可验证的运行判据。
- **类型一致性**:DTO(`DocumentSummary`/`SearchResult`/`DocumentDetail`/`ImportOutcome`/`SourceFileMeta`/`ExportSummary`)在 Rust(dto.rs)与 TS(types.ts)字段逐一对应;命令名(`list_timeline`/`search`/`get_document`/`import_paths`/`read_source_bytes`/`export_vault`)在 commands.rs 注册、api.ts 调用、Task 5/6 一致。`root_join`(T5 note)在 core-model 新增并被 `read_source_bytes` 使用。
- **依赖方向**:pipeline → core-model + parser(合法);desktop/src-tauri → core-model + pipeline;UI → 仅 api.ts。无环。
```
